import { Route, Switch, BrowserRouter } from "react-router-dom";

const Routes = () => {
    return (
        <BrowserRouter>
            <Switch>
                <Route path="/" component={}/>
            </Switch>
        </BrowserRouter>
    )
}

export default Routes;